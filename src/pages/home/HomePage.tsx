const HomePage = () => {
  // const { user } = useUserStore();
  // const navigate = useNavigate();

  // if (!user) {
  //   navigate("/login");
  // }

  return <div>Home page</div>;
};

export default HomePage;
